-- criação barra de dados
create or replace database fatec;
--seleciona a barra de dados ativa
use fatec;
-- or replace apaga tudo
create or replace table produtos(
    codigo smallint unsigned,
    nome varchar(100),
    quantidade tinyint 


);

